import { AutomaticFocusDirective } from './automatic-focus.directive';

describe('AutomaticFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new AutomaticFocusDirective();
    expect(directive).toBeTruthy();
  });
});
