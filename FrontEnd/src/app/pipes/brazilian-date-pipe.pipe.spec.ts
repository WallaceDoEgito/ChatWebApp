import { BrazilianDatePipePipe } from './brazilian-date-pipe.pipe';

describe('BrazilianDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new BrazilianDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
